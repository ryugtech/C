#include <stdio.h>

int main()
{
    int x, y, result;
    printf("분자와 분모를 입력하시오:");
    scanf("%d %d", &x, &y);

    if(y != 0)
    {
        result = x / y;
        printf("결과는 %d입니다", result);
    }
    else
    {
        result = 0;
        printf("0으로 나눌 수는 없습니다.");
    }
}
